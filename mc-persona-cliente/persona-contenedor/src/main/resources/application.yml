server:
  port: 8181
  error:
    whitelabel:
      enabled: false

logging:
  level:
    ec.devsu.app: DEBUG

spring:
  mvc:
    view:
      prefix: ""  # Evita que Spring busque vistas en rutas específicas
      suffix: ""
  web:
    resources:
      add-mappings: false
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/devsu
    username: postgres
    password: seya1922
    driver-class-name: org.postgresql.Driver

